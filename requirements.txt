# Requirements for GitHub Actions CI/CD
# This project uses pyproject.toml for dependency management
# This file provides compatibility for build systems expecting requirements.txt

# Install the vespera-scriptorium package with dev dependencies
-e packages/vespera-scriptorium[dev]

# Core dependencies (these are already specified in pyproject.toml but listed for clarity)
mcp>=1.9.0
pydantic>=2.0.0
jinja2>=3.1.0
pyyaml>=6.0.0
aiofiles>=23.0.0
psutil>=5.9.0
filelock>=3.12.0
sqlalchemy>=2.0.0
alembic>=1.10.0
aiosqlite>=0.19.0
typer>=0.9.0
rich>=13.0.0
watchfiles>=0.21.0

# Development dependencies (also in pyproject.toml)
pytest>=7.0.0
pytest-asyncio>=0.23.0
pytest-timeout>=2.1.0
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
mypy>=1.0.0
coverage>=7.0.0
pre-commit>=3.0.0